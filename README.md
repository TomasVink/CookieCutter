# OpenSCAD project to create cookie cutters from svg files
Use a svg file, preferably with a single object in it. Edit the `.scad` file to point to the right svg file. Run OpenSCAD to get the `.stl`.
